# AXIOM PULSE Test Build

The root application is now the approved AXIOM Pulse login surface. Authentication is shown immediately; there is no Enter AXIOM Pulse gate before credentials.

The Master account remains a real Supabase master role. Master can test FSM and Salesperson authority without mutating the authenticated role.
